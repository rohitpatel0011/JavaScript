let a = new Array("hello", "Rohit", "patel");
let x = 34;
let p = "hello";
// let b = Array.isArray(a);
//Arrays.isArray is checking the variable is array or not
let y = Array.isArray(x);
let q = Array.isArray(p);
console.log(b);
console.log(y);
console.log(q);
//-----if -else condition print value---
if (Array.isArray(a)) {
    console.log("yes, this is an arrays");
} else {
    console.log("No, This is not an Arrays");
}
//------------------------------
if (Array.isArray(x)) {
    console.log("yes, this is an arrays");
} else {
    console.log("No, This is not an Arrays");
}
//--------------------------------------
if (Array.isArray(p)) {
    console.log("yes, this is an arrays");
} else {
    console.log("No, This is not an Arrays");
}